---
phase: 05-ai-weather-assistant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/tools.ts
  - src/lib/ai/system-prompt.ts
  - src/app/api/chat/route.ts
autonomous: true
requirements: [AI-01, AI-02, AI-03]
---

<objective>
Wire Claude API with tool use — register all MCP tools as Claude tools, create system prompt, and build the chat API route that handles tool calling loops.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Define Claude tool schemas</name>
  <files>src/lib/ai/tools.ts</files>
  <action>
  Define Claude tool definitions for all MCP tools:
  - get_current_weather(lat, lon)
  - get_forecast(lat, lon, type)
  - get_marine_weather(lat, lon)
  - get_nearby_buoys(lat, lon, radius)
  - get_buoy_observations(stationId)
  - get_buoy_history(stationId)
  - resolve_location(query)

  Also create a tool executor function that dispatches tool calls to the actual MCP functions.
  </action>
</task>

<task type="auto">
  <name>Task 2: Create system prompt</name>
  <files>src/lib/ai/system-prompt.ts</files>
  <action>
  Create a system prompt that:
  - Identifies the AI as a weather intelligence assistant
  - Requires tool use for ALL weather claims (never make up data)
  - Includes current location context
  - Guides activity-specific advice
  </action>
</task>

<task type="auto">
  <name>Task 3: Build chat API route</name>
  <files>src/app/api/chat/route.ts</files>
  <action>
  POST endpoint that:
  - Accepts { messages, lat, lon, locationName }
  - Sends to Claude with tools and system prompt
  - Handles tool calling loop (max 5 iterations)
  - Returns final assistant message
  </action>
</task>
</tasks>
