---
phase: 06-polish-and-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/chat/route.ts
  - src/components/chat/ChatPanel.tsx
autonomous: true
requirements: [AI-04]
---

<objective>
Implement streaming AI responses using Server-Sent Events so characters appear in real-time as Claude generates them.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Update chat API to stream</name>
  <files>src/app/api/chat/route.ts</files>
  <action>
  Modify the chat route to use Claude streaming API and return an SSE stream.
  Handle tool use within the stream (collect tool calls, execute, resume streaming).
  </action>
</task>

<task type="auto">
  <name>Task 2: Update ChatPanel to consume stream</name>
  <files>src/components/chat/ChatPanel.tsx</files>
  <action>
  Update ChatPanel to read from the SSE stream and render characters incrementally.
  </action>
</task>
</tasks>
