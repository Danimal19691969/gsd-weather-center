---
phase: 03-map-and-location
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/context/LocationContext.tsx
  - src/components/map/WeatherMap.tsx
  - src/components/Dashboard.tsx
autonomous: true
requirements: [UI-03]

must_haves:
  truths:
    - "LocationProvider wraps the dashboard and provides lat/lon + setter"
    - "All weather panels consume location from context instead of hardcoded props"
    - "Mapbox map renders with dark styling consistent with HUD theme"
    - "Map viewport follows the current location"
  artifacts:
    - path: "src/lib/context/LocationContext.tsx"
      provides: "LocationProvider and useLocation hook"
      exports: ["LocationProvider", "useLocation"]
    - path: "src/components/map/WeatherMap.tsx"
      provides: "Mapbox GL map with dark style"
  key_links: []
---

<objective>
Create a LocationProvider context to manage the current dashboard location (replacing hardcoded lat/lon), and integrate a Mapbox GL map with dark styling into the dashboard layout.

Purpose: Centralizes location state so all panels react to location changes, and adds the interactive map.
Output: Dashboard renders a dark-styled Mapbox map and all panels use context-driven location.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create LocationProvider context</name>
  <files>src/lib/context/LocationContext.tsx</files>
  <action>
  Create a React Context that provides:
  - `lat: number`, `lon: number` — current coordinates
  - `locationName: string` — display name (e.g., "Portland, OR")
  - `setLocation(lat, lon, name)` — update function

  Default to Portland, OR (45.5152, -122.6784).
  Export `LocationProvider` and `useLocation()` hook.
  </action>
</task>

<task type="auto">
  <name>Task 2: Create WeatherMap component</name>
  <files>src/components/map/WeatherMap.tsx</files>
  <action>
  Create a Mapbox GL map using react-map-gl:
  - Use `mapbox://styles/mapbox/dark-v11` as the base style
  - Map viewport centers on current location from context
  - On map move/zoom, update viewport state locally (don't change location context)
  - Wrap in Panel component with "Tactical Map" title
  - Set initial zoom to 8
  - Use NEXT_PUBLIC_MAPBOX_TOKEN from env
  - Show graceful fallback if token is missing
  </action>
</task>

<task type="auto">
  <name>Task 3: Integrate into Dashboard</name>
  <files>src/components/Dashboard.tsx</files>
  <action>
  - Wrap Dashboard content in LocationProvider
  - Replace hardcoded lat/lon with useLocation() hook
  - Add WeatherMap to the grid layout (below header, above weather panels)
  - Update header to show locationName from context
  </action>
</task>

</tasks>
