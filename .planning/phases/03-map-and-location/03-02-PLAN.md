---
phase: 03-map-and-location
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/map/LocationSearch.tsx
  - src/components/Dashboard.tsx
autonomous: true
requirements: [LOC-01]

must_haves:
  truths:
    - "User can type a city name and see autocomplete suggestions"
    - "Selecting a suggestion updates the dashboard location context"
    - "All weather panels update to the new location"
  artifacts:
    - path: "src/components/map/LocationSearch.tsx"
      provides: "Search input with geocoding autocomplete"
  key_links: []
---

<objective>
Build a location search input with geocoding autocomplete that updates the dashboard location when a result is selected.

Purpose: Users can search for any location and see weather data for it (LOC-01).
Output: Search bar in the header that queries the geocoding API and updates all panels.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Build LocationSearch component</name>
  <files>src/components/map/LocationSearch.tsx</files>
  <action>
  Create a search input component:
  - Text input with HUD styling (dark bg, cyan border, mono font)
  - On typing (debounced 300ms), call `/api/location?q=` to get suggestions
  - Show dropdown of results below input
  - On selecting a result, call setLocation from context with coordinates and place name
  - Close dropdown on selection or blur
  - Show loading indicator while fetching
  </action>
</task>

<task type="auto">
  <name>Task 2: Add LocationSearch to Dashboard header</name>
  <files>src/components/Dashboard.tsx</files>
  <action>
  Add the LocationSearch component to the dashboard header area, between the title and the OPERATIONAL status indicator.
  </action>
</task>

</tasks>
