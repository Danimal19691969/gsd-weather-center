---
phase: 03-map-and-location
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/map/GeolocationButton.tsx
  - src/components/Dashboard.tsx
autonomous: true
requirements: [LOC-02]

must_haves:
  truths:
    - "User can click 'Use My Location' and dashboard updates to their browser location"
    - "Permission denial shows a visible message, falls back to default"
    - "Button has HUD styling consistent with the theme"
  artifacts:
    - path: "src/components/map/GeolocationButton.tsx"
      provides: "Browser geolocation button with permission handling"
  key_links: []
---

<objective>
Add a "Use My Location" button that uses the browser Geolocation API to set the dashboard location, with graceful fallback when permission is denied.

Purpose: Users can quickly set their location without searching (LOC-02).
Output: Geolocation button in the header next to the search bar.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Build GeolocationButton component</name>
  <files>src/components/map/GeolocationButton.tsx</files>
  <action>
  Create a button component:
  - Uses navigator.geolocation.getCurrentPosition
  - On success: calls setLocation with browser coordinates, uses reverse geocoding or simple "My Location" label
  - On error/denied: shows brief error message (toast-style or inline), keeps current location
  - Loading state while acquiring position
  - HUD styling: border, mono font, cyan accent
  </action>
</task>

<task type="auto">
  <name>Task 2: Add GeolocationButton to Dashboard</name>
  <files>src/components/Dashboard.tsx</files>
  <action>
  Add the GeolocationButton next to the LocationSearch in the header.
  </action>
</task>

</tasks>
