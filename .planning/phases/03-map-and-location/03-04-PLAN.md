---
phase: 03-map-and-location
plan: 04
type: execute
wave: 3
depends_on: [03-01]
files_modified:
  - src/components/map/BuoyMarkers.tsx
  - src/components/map/WeatherMap.tsx
  - src/lib/hooks/useBuoys.ts
autonomous: true
requirements: [BUOY-04]

must_haves:
  truths:
    - "Buoy stations appear as markers on the map"
    - "Markers cluster when zoomed out"
    - "Clicking a marker shows a popup with station name and key readings"
    - "Markers update when location changes (nearby buoys)"
  artifacts:
    - path: "src/components/map/BuoyMarkers.tsx"
      provides: "Buoy marker layer with clustering and popups"
    - path: "src/lib/hooks/useBuoys.ts"
      provides: "SWR hook for nearby buoys"
  key_links: []
---

<objective>
Add buoy station markers to the Mapbox map with clustering and click-to-popup showing key readings.

Purpose: Users can see buoy locations spatially and inspect their data (BUOY-04).
Output: Buoy markers on the map that cluster and show popups with readings on click.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create useBuoys hook</name>
  <files>src/lib/hooks/useBuoys.ts</files>
  <action>
  Create SWR hook `useNearbyBuoys(lat, lon)` that fetches from `/api/buoys?tool=nearby&lat=&lon=&radius=200`.
  Use same SWR_OPTIONS pattern as useWeather hooks.
  </action>
</task>

<task type="auto">
  <name>Task 2: Create BuoyMarkers component</name>
  <files>src/components/map/BuoyMarkers.tsx</files>
  <action>
  Create a react-map-gl layer component:
  - Fetch nearby buoys using useNearbyBuoys hook
  - Convert buoy stations to GeoJSON FeatureCollection
  - Use Source + Layer from react-map-gl for circle markers (cyan color)
  - Add clustering with cluster-count labels
  - On marker click, show a Popup with station name, distance, and type
  - Style markers to match HUD theme (cyan dots)
  </action>
</task>

<task type="auto">
  <name>Task 3: Integrate BuoyMarkers into WeatherMap</name>
  <files>src/components/map/WeatherMap.tsx</files>
  <action>
  Add BuoyMarkers as a child of the Map component in WeatherMap.
  </action>
</task>

</tasks>
