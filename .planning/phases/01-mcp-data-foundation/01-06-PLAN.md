---
phase: 01-mcp-data-foundation
plan: 06
type: execute
wave: 4
depends_on: ["01-05"]
files_modified:
  - src/lib/__tests__/weather.test.ts
  - src/lib/__tests__/buoys.test.ts
  - src/lib/__tests__/location.test.ts
  - src/lib/__tests__/cache.test.ts
autonomous: true
requirements: [INFRA-01, INFRA-03, INFRA-04]

must_haves:
  truths:
    - "All three MCP tool servers return well-formed ToolResult for valid inputs"
    - "NDBC parser handles real station data including MM and sentinel values"
    - "Cache deduplication and TTL expiry work correctly"
    - "Error cases return structured errors, never throw"
  artifacts:
    - path: "src/lib/__tests__/weather.test.ts"
      provides: "Integration tests for weather tool functions"
      min_lines: 40
    - path: "src/lib/__tests__/buoys.test.ts"
      provides: "Integration tests for buoy tool functions + NDBC parser"
      min_lines: 60
    - path: "src/lib/__tests__/location.test.ts"
      provides: "Integration tests for geocoding tool function"
      min_lines: 30
    - path: "src/lib/__tests__/cache.test.ts"
      provides: "Unit tests for cache layer"
      min_lines: 40
  key_links: []
---

<objective>
Write integration tests against live APIs to validate all three MCP tool servers return well-formed data, and unit tests for the cache layer. Tests run against real endpoints — no mocks.

Purpose: Proves the entire data layer works end-to-end before building UI on top of it. Catches parsing bugs, API changes, and edge cases early.
Output: Test suite covering all tool functions and the cache layer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mcp-data-foundation/01-CONTEXT.md
@src/lib/tools/weather.ts
@src/lib/tools/buoys.ts
@src/lib/tools/location.ts
@src/lib/cache.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up test runner</name>
  <files>package.json</files>
  <action>
  Install a test runner (vitest recommended — works well with TypeScript and Next.js) and add a `test` script to package.json.

  Install: `vitest` as a dev dependency.
  Add script: `"test": "vitest run"` and `"test:watch": "vitest"` to package.json.
  Create a minimal vitest config if needed (vitest.config.ts) with path alias support matching tsconfig.
  </action>
  <verify>npx vitest run --passWithNoTests succeeds</verify>
  <done>Test runner installed and configured</done>
</task>

<task type="auto">
  <name>Task 2: Write weather tool integration tests</name>
  <files>src/lib/__tests__/weather.test.ts</files>
  <action>
  Write integration tests that call weather tools against live Open-Meteo API.

  Use Portland, OR (45.5, -122.6) as the test location.

  **Tests:**
  - `getCurrentWeather` returns success with temperature, humidity, windSpeed, description
  - `getCurrentWeather` returns all expected fields as numbers (not null or undefined)
  - `getCurrentWeather` description is a non-empty string
  - `getForecast` with type='daily' returns 7 daily entries with tempMax, tempMin
  - `getForecast` with type='hourly' returns 48 hourly entries with temperature
  - `getMarineWeather` returns success for a coastal location (46.16, -124.49)
  - `getMarineWeather` handles gracefully for an inland location (45.5, -122.6)
  - All functions return ToolResult shape (success field exists)

  Set test timeout to 15 seconds (live API calls).
  </action>
  <verify>All weather tests pass with live data</verify>
  <done>Weather tool tests pass, validating real API responses</done>
</task>

<task type="auto">
  <name>Task 3: Write buoy tool integration tests</name>
  <files>src/lib/__tests__/buoys.test.ts</files>
  <action>
  Write integration tests for buoy tools and the NDBC parser.

  **NDBC Parser unit tests (can use inline test data):**
  - Parses a sample NDBC text block into BuoyObservation objects
  - Maps MM values to null
  - Maps sentinel values (99.0, 999.0) to null
  - Handles empty input gracefully
  - Extracts correct timestamp from YY MM DD hh mm columns

  **Integration tests (live NOAA API):**
  - `getBuoyObservations("46029")` returns success with wave/wind data (Columbia River Bar)
  - `getBuoyObservations` result has stationId matching input
  - `getBuoyObservations` with invalid station ID returns structured error
  - `getBuoyHistory("46029")` returns array of observations
  - `getBuoyHistory` observations are within last 24 hours
  - `getNearbyBuoys(45.5, -122.6, 150)` returns at least one buoy
  - `getNearbyBuoys` results are sorted by distance ascending
  - `getNearbyBuoys` results all have distanceMiles field

  **Haversine distance tests:**
  - Portland to Columbia River Bar (~95-100 miles)
  - Same point returns 0
  - New York to London (~3450 miles, sanity check)

  Set test timeout to 15 seconds for live API tests.
  </action>
  <verify>All buoy tests pass, NDBC parser handles real and edge case data</verify>
  <done>Buoy tool tests pass, parser validated with real NDBC data</done>
</task>

<task type="auto">
  <name>Task 4: Write location tool integration tests</name>
  <files>src/lib/__tests__/location.test.ts</files>
  <action>
  Write integration tests for the geocoding tool against live Mapbox API.

  **Tests:**
  - `resolveLocation("Portland, OR")` returns success with at least one result
  - First result has coordinates near 45.5, -122.6 (within 0.5 degrees)
  - First result has a non-empty placeName
  - `resolveLocation("97201")` returns success with Portland-area coordinates
  - `resolveLocation` returns relevance scores between 0 and 1
  - Empty/gibberish query returns success with empty or near-empty array
  - All results have valid lat/lon (lat: -90 to 90, lon: -180 to 180)

  Note: These tests require NEXT_PUBLIC_MAPBOX_TOKEN in environment. Skip gracefully if token not available.

  Set test timeout to 10 seconds.
  </action>
  <verify>All location tests pass (with Mapbox token configured)</verify>
  <done>Location tool tests pass, geocoding returns valid coordinates</done>
</task>

<task type="auto">
  <name>Task 5: Write cache unit tests</name>
  <files>src/lib/__tests__/cache.test.ts</files>
  <action>
  Write unit tests for the cache layer (no live API calls needed).

  **Tests:**
  - cachedFetch returns fetcher result on cache miss
  - cachedFetch returns cached result on cache hit (fetcher not called again)
  - cachedFetch returns fresh data after TTL expires
  - Concurrent calls to same key only invoke fetcher once (deduplication)
  - Failed fetcher does not cache the error
  - clearCache() removes all entries
  - clearCache(prefix) removes only matching entries

  Use short TTL (100ms) for expiry tests and fake/simple fetcher functions.
  </action>
  <verify>All cache tests pass</verify>
  <done>Cache layer tested: TTL, deduplication, error handling all verified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run` passes all tests
- [ ] `npx tsc --noEmit` passes
- [ ] Weather tests validate real Open-Meteo responses
- [ ] Buoy tests validate real NDBC data parsing
- [ ] NDBC parser handles MM and sentinel values in tests
- [ ] Haversine distance tests pass with known distances
- [ ] Location tests validate Mapbox geocoding (or skip gracefully without token)
- [ ] Cache tests verify TTL, deduplication, and error exclusion
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Full test suite runs green
- Tests prove the data layer is reliable before building UI
</success_criteria>

<output>
After completion, create `.planning/phases/01-mcp-data-foundation/01-06-SUMMARY.md`
</output>
