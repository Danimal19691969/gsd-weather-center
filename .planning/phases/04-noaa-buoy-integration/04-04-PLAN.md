---
phase: 04-noaa-buoy-integration
plan: 04
type: execute
wave: 3
depends_on: [04-01]
files_modified:
  - src/components/buoy/BuoyHistoryChart.tsx
  - src/lib/hooks/useBuoys.ts
  - src/components/Dashboard.tsx
autonomous: true
requirements: [BUOY-05]
---

<objective>
Build 24-hour history charts for buoy readings using Recharts.

Purpose: Users can see trends in buoy data over the last 24 hours (BUOY-05).
Output: Time-series line charts for wave height, water temp, wind speed, and pressure.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add useBuoyHistory hook</name>
  <files>src/lib/hooks/useBuoys.ts</files>
  <action>
  Add useBuoyHistory(stationId: string | null) hook that fetches /api/buoys?tool=history&stationId=.
  Only fetch when stationId is not null.
  </action>
</task>

<task type="auto">
  <name>Task 2: Build BuoyHistoryChart</name>
  <files>src/components/buoy/BuoyHistoryChart.tsx</files>
  <action>
  Create a panel with Recharts line charts:
  - Show 4 small charts: wave height, water temp, wind speed, pressure
  - Each chart uses ResponsiveContainer, LineChart, Line, XAxis, YAxis, Tooltip
  - X-axis shows time (formatted hours)
  - HUD-themed colors (cyan lines, dark bg)
  - Loading state, "no data" when no buoy selected
  </action>
</task>

<task type="auto">
  <name>Task 3: Add to Dashboard</name>
  <files>src/components/Dashboard.tsx</files>
  <action>
  Add BuoyHistoryChart below the buoy observation section.
  </action>
</task>
</tasks>
