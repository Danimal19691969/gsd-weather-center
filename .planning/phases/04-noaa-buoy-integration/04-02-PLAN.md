---
phase: 04-noaa-buoy-integration
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/buoy/BuoyObservationCard.tsx
  - src/lib/hooks/useBuoys.ts
  - src/components/Dashboard.tsx
autonomous: true
requirements: [BUOY-01]
---

<objective>
Build a live buoy observation display card showing all readings for a selected station.

Purpose: Users can see live buoy data for a selected station (BUOY-01).
Output: Card showing wave height, wave period, wind, water temp, etc.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add useBuoyObservation hook</name>
  <files>src/lib/hooks/useBuoys.ts</files>
  <action>
  Add useBuoyObservation(stationId: string | null) hook that fetches /api/buoys?tool=observations&stationId=.
  Only fetch when stationId is not null.
  </action>
</task>

<task type="auto">
  <name>Task 2: Build BuoyObservationCard</name>
  <files>src/components/buoy/BuoyObservationCard.tsx</files>
  <action>
  Create a Panel that displays:
  - Station name/ID in header
  - StatBlocks for: wave height (m), wave period (s), wind speed (m/s), wind direction,
    water temp (°C), air temp (°C), pressure (hPa), gust speed (m/s)
  - Timestamp of observation
  - Loading/error states
  - Shows "Select a buoy" message when no buoy selected
  </action>
</task>

<task type="auto">
  <name>Task 3: Add to Dashboard</name>
  <files>src/components/Dashboard.tsx</files>
  <action>
  Add BuoyObservationCard next to BuoyListPanel in a grid layout.
  </action>
</task>
</tasks>
