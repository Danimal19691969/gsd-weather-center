---
phase: 04-noaa-buoy-integration
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/buoy/BuoyDetailView.tsx
  - src/components/Dashboard.tsx
autonomous: true
requirements: [BUOY-03]
---

<objective>
Build an expanded buoy detail view with full station readings and metadata.

Purpose: Users can inspect complete buoy data and station info (BUOY-03).
Output: Detail panel with all observation fields and station metadata.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build BuoyDetailView</name>
  <files>src/components/buoy/BuoyDetailView.tsx</files>
  <action>
  Create a comprehensive detail view that:
  - Shows station metadata (name, ID, type, coordinates)
  - Shows ALL observation fields from BuoyObservation in a organized grid
  - Groups readings: Waves (height, period, avg period, direction), Wind (speed, direction, gust),
    Atmosphere (air temp, pressure, dewpoint, visibility), Ocean (water temp)
  - Uses StatBlock components for consistent formatting
  - Shows when no buoy is selected
  This will be integrated into BuoyObservationCard rather than as a separate panel,
  making 04-02's card the full detail view.
  </action>
</task>
</tasks>
