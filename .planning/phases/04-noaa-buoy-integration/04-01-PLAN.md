---
phase: 04-noaa-buoy-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/buoy/BuoyListPanel.tsx
  - src/components/Dashboard.tsx
autonomous: true
requirements: [BUOY-02]
---

<objective>
Build a buoy list panel showing nearby stations with key stats, with the ability to select a station for detail viewing.

Purpose: Users can discover and browse nearby buoy stations (BUOY-02).
Output: Panel showing nearby buoys sorted by distance with key readings.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Create buoy selection context</name>
  <files>src/lib/context/BuoyContext.tsx</files>
  <action>
  Create a simple context to track the currently selected buoy station ID.
  - selectedBuoyId: string | null
  - setSelectedBuoy(id: string | null): void
  Export BuoyProvider and useSelectedBuoy hook.
  </action>
</task>

<task type="auto">
  <name>Task 2: Build BuoyListPanel</name>
  <files>src/components/buoy/BuoyListPanel.tsx</files>
  <action>
  Create a panel that:
  - Uses useNearbyBuoys to get stations near current location
  - Displays a scrollable list of buoy stations
  - Each row shows: station name, ID, distance, type
  - Clicking a row selects the buoy (sets selectedBuoyId)
  - Selected row highlighted with accent color
  - Loading/error states
  </action>
</task>

<task type="auto">
  <name>Task 3: Integrate into Dashboard</name>
  <files>src/components/Dashboard.tsx</files>
  <action>
  Add BuoyProvider wrapping dashboard content.
  Add BuoyListPanel to the layout below the marine panel.
  </action>
</task>
</tasks>
